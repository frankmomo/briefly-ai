# ============================================================
# .env.local — Variables de entorno para Next.js frontend
# ============================================================
# Copia como .env.local en web/

# URL del backend API (localhost en dev, dominio publico de Railway en prod)
NEXT_PUBLIC_API_URL=http://localhost:3001
